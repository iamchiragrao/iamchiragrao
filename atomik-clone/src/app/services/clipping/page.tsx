import { PageTemplate } from "@/components/PageTemplate";

export default function Clipping() {
  return (
    <PageTemplate 
      title="Clipping" 
      subtitle="High-performance short-form content at scale."
    />
  );
}
